#ifndef HEADER_PARTITION
#define HEADER_PARTITION
typedef struct {
	int size; //size of the partition
	int id; //partition id number
	int runtime; //maintains the amount of time programs have been running
} Partition;
#endif
